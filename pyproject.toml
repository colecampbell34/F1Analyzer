[tool.ruff]
target-version = "py39"
line-length = 120
extend-exclude = [
    "ai_cache",
    "dist",
    "f1_cache",
    "feedback",
    "node_modules",
    "static_schedule.py",
]

[tool.ruff.lint]
select = [
    "E9",
    "F",
]
